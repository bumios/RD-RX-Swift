# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RD-RX-Swift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RD-RX-Swift
  pod 'RxSwift', '6.5.0'
  pod 'RxCocoa', '6.5.0'

  target 'RD-RX-SwiftTests' do
    inherit! :search_paths
  end

  target 'RD-RX-SwiftUITests' do
    # Pods for testing
  end

end
